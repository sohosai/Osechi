- ユーザー環境におけるエラーが発生したときに迅速にエラーに対応できるようにするためにログを出力することにする。

- ログの出力には[tracing](https://docs.rs/tracing/latest/tracing/)を使っており、非同期な出力に対応しているため、基本的にブロッキングは発生しない。

- `#[instrument]`を用いたトレースは基本的に行わない。

- `error!`, `warn!`, `info!`,`debug!`のみを使用すること。